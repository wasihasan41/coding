#include <stdio.h>

int main(){

    int length, breadth;
    printf("write length and breadth\n");
    scanf("%d %d", &length, &breadth);

    printf("area of rectangle is %d", length * breadth);

    return 0;
}